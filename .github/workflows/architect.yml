name: Strategy Architect
on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: strategy-architect
  cancel-in-progress: true

jobs:
  evaluate-and-plan:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Auto-Close Conflicted PRs
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          gh pr list --state open --json number,mergeable > open_prs.json || echo "[]" > open_prs.json
          CONFLICTED_PRS=$(jq -r '.[] | select(.mergeable == "CONFLICTING") | .number' open_prs.json)
          for pr in $CONFLICTED_PRS; do
            if [ -n "$pr" ]; then
              echo "Closing conflicted PR #$pr..."
              gh pr close "$pr" -d -c "Autonomously closed by Strategy Architect: PR has unresolvable merge conflicts. Branch deleted to reset state." || true
            fi
          done

      - name: Fetch Open Issues
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          gh issue list --state open --json number,title > open_issues.json || echo "[]" > open_issues.json

      - name: Build Codebase Overview
        run: |
          (
            echo "You are the Lead Architect for a Screeps bot."
            echo "Analyze the current directory structure, AGENTS.md phase milestones, and CURRENT OPEN ISSUES."
            echo "**EVALUATION DIRECTIVES:**"
            echo "1. CLOSING ISSUES: If an open issue asks to build a system, and the corresponding file now exists in the CURRENT DIRECTORY TREE, you MUST add its issue number to 'issues_to_close'."
            echo "2. NO DUPLICATES: DO NOT propose new issues for anything already in CURRENT OPEN ISSUES."
            echo "3. DECOUPLED PARALLELISM: When proposing new work, NEVER ask to modify central files like 'src/main.js' or 'src/colonies/colonyManager.js' across multiple issues. Parallel issues must ONLY create isolated, standalone modules."
            echo "4. INTEGRATION: If standalone modules exist but aren't being called, propose a SINGLE 'Integration' issue to wire them into main.js or other central managers."
            echo "5. Identify up to 3 distinct, independent missing modules or 1 integration task."
            echo "**CRITICAL CONSTRAINTS:**"
            echo "- Keep the scope of each new issue limited to 1-3 files maximum."
            echo "- Provide strict acceptance criteria for each."
            echo "--- OPEN ISSUES ---"
            cat open_issues.json
            echo ""
            echo "--- AGENTS.md ---"
            cat AGENTS.md 2>/dev/null || echo "[AGENTS.md NOT FOUND]"
            echo ""
            echo "--- CURRENT DIRECTORY TREE ---"
            find src -type f | sort || echo "No files found."
          ) > prompt_context.txt

      - name: Generate Action Plan JSON
        id: generate-plan
        run: |
          jq -n --rawfile text prompt_context.txt '{
            contents: [{parts: [{"text": $text}]}], 
            generationConfig: {
              temperature: 0.1,
              responseMimeType: "application/json",
              responseSchema: {
                type: "OBJECT",
                properties: {
                  issues_to_close: { 
                    type: "ARRAY", 
                    items: { type: "INTEGER" },
                    description: "List of issue numbers that are resolved or obsolete."
                  },
                  create_new: { 
                    type: "BOOLEAN",
                    description: "True if new independent issues should be created, false if we should wait."
                  },
                  new_issues: {
                    type: "ARRAY",
                    description: "List of new, distinct, non-overlapping issues to create.",
                    items: {
                      type: "OBJECT",
                      properties: {
                        title: { type: "STRING" },
                        body: { type: "STRING", description: "Detailed Markdown body with acceptance criteria." },
                        files_to_touch: { type: "ARRAY", items: { type: "STRING" } }
                      },
                      required: ["title", "body", "files_to_touch"]
                    }
                  }
                },
                required: ["issues_to_close", "create_new"]
              }
            }
          }' > payload.json
          
          MODELS=("gemini-2.5-flash" "gemini-3.1-flash-lite")
          SUCCESS=false
          MAX_RETRIES=3
          
          for MODEL in "${MODELS[@]}"; do
            for ((i=1; i<=MAX_RETRIES; i++)); do
              HTTP_RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "https://generativelanguage.googleapis.com/v1beta/models/$MODEL:generateContent?key=${{ secrets.GEMINI_API_KEY }}" \
                -H 'Content-Type: application/json' \
                -d @payload.json)
                
              HTTP_BODY=$(echo "$HTTP_RESPONSE" | sed '$d')
              HTTP_STATUS=$(echo "$HTTP_RESPONSE" | tail -n 1)
              
              if [ "$HTTP_STATUS" -eq 200 ]; then
                CLEAN_TEXT=$(echo "$HTTP_BODY" | jq -r '.candidates[0].content.parts[0].text // empty')
                if [[ -n "$CLEAN_TEXT" && "$CLEAN_TEXT" != "null" ]]; then
                  SUCCESS=true
                  echo "$CLEAN_TEXT" > generated_plan.json
                  break 2
                fi
              fi
              sleep 5
            done
          done
          
          if [ "$SUCCESS" = false ]; then
            echo "::error::Failed to generate action plan."
            exit 1
          fi

      - name: Execute Action Plan
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          # 1. Close obsolete issues (Strict array parsing to prevent jq null iteration failures)
          ISSUES_TO_CLOSE=$(jq -r 'if .issues_to_close == null then [] else .issues_to_close end | join(" ")' generated_plan.json)
          for issue in $ISSUES_TO_CLOSE; do
            if [ -n "$issue" ]; then
              echo "Closing obsolete issue #$issue..."
              gh issue close "$issue" -c "Autonomously closed by Strategy Architect: Evaluated as resolved or obsolete." || true
            fi
          done

          # 2. Evaluate if we need to create new issues
          CREATE_NEW=$(jq -r '.create_new' generated_plan.json)
          if [ "$CREATE_NEW" == "true" ]; then
            # Ensure labels exist to prevent creation failure
            gh label create "architect-proposal" --color "0E8A16" --force || true
            gh label create "jules-approved" --color "0052cc" --force || true
            
            # Loop through the array of new issues safely
            jq -c 'if .new_issues == null then [] else .new_issues end | .[]?' generated_plan.json | while read -r issue; do
              TITLE=$(echo "$issue" | jq -r '.title')
              FILES=$(echo "$issue" | jq -r 'if .files_to_touch == null then [] else .files_to_touch end | join(", ")')
              BODY=$(echo "$issue" | jq -r '.body')
              
              FULL_BODY=$(printf "**Target Files:** %s\n\n%s" "$FILES" "$BODY")
              
              echo "Creating and auto-approving new issue: $TITLE"
              gh issue create --title "[Architect] $TITLE" --body "$FULL_BODY" --label "architect-proposal,jules-approved"
            done
          else
            echo "Architect determined no new issues are required at this time."
          fi