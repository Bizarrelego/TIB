## 2024-06-03 - [CPU Optimization] Learning: Native `{}` objects in V8 Javascript incur hidden performance penalties (monomorphism loss and hashing overhead) when used as frequently written/read key-value stores for hundreds of creeps. Action: Convert high-frequency dictionary objects like `global.creepHeap`, `global.Tick.gatherClaims`, and `global.Tick.deliveryClaims` into explicit `new Map()` structures to guarantee mathematical O(1) performance and stable memory utilization.
## 2026-06-05 - [IntelManager Optimization] Learning: Native Map objects cannot be serialized properly into Screeps global Memory structure via JSON.stringify cross-tick, requiring standard POJOs for persistence despite the v24 architecture focus. Hardcoding strings for ownership checks is brittle. Action: Use standard JS objects for global Memory persistence, and dynamically fetch ownership via !s.my.
## 2026-10-26 - [EventLog Radar Optimization] Learning: Repeated use of `room.find(FIND_HOSTILE_CREEPS)` is an expensive O(N) operation to run every tick. Action: Implement a radar system that scans `room.getEventLog()` for `EVENT_ATTACK` or `EVENT_HEAL`. Only execute `room.find` if an event is detected, known hostiles exist in the room, or periodically (e.g. `Game.time % 13 === 0`) to catch passive intruders. This reduces array iterations from O(N) to O(1) mathematically.
## 2026-06-07 - [GameObject Utility Optimization] Learning: [Replacing repeated native Game.getObjectById calls with a per-tick caching Map() utility across all execution roles reduces the costly JS to C++ cross-boundary serialization overhead by caching both successful object retrievals and null lookups for dead entities.] Action: [Ensure all newly created utility or manager files that retrieve Game objects leverage the central GameObjectUtility.getById caching mechanism to prevent redundant API polling within the same game tick.]
## 2026-06-08 - [CPU Optimization] Learning: The Screeps engine's native `moveTo()` function inherently writes pathing arrays directly to `creep.memory._move`, forcing massive string array serialization cross-tick. Action: Implemented a memory-proxy architecture inside `RoleExecutor` that moves the path to `creep.heap.path` immediately after logic execution and deletes it from native memory, then restores it just before execution. This eliminates the JSON.stringify bottleneck while maintaining native `moveTo()` path caching behavior.
