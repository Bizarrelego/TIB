## BOLT'S JOURNAL
## 2026-05-30 - [Engine API Abuse] Learning: Firing native intents (like pickup or transfer) when creeps are out of range returns ERR_NOT_IN_RANGE but wastes CPU evaluating the intent. Action: Prevent the native intent call by checking range first via creep.pos.getRangeTo(target) > 1, falling back to moveTo.
## 2026-05-31 - [Empty Source Optimization] Learning: Native API intent evaluation wastes CPU when a source is depleted. Action: Calculate ticksToRegeneration, store in creep.heap.sleepUntil, and return early from execution loops until that tick is reached.
## 2024-06-01 - HaulerTaskProvider Learning: Safely Iterating Global Collections Action: When iterating over collections like `global.State.creeps` or fallback `Game.creeps`, robustly handle both standard Objects and Maps (e.g., checking type or using `Object.values()` vs `.values()`) to prevent iteration crashes. Always ensure pure utility files are only imported when strictly required or explicitly mentioned to avoid MODULE_NOT_FOUND errors in Screeps.
## 2026-06-01 - [Execution Pipeline] Learning: Managers like SpawnManager counting creeps and MemoryCleanupManager cleaning up stale creeps every tick wastes significant CPU when this state only changes occasionally. Action: Implement tick-slicing via modulo checks (e.g. if (Game.time % 10 !== 0) return;) to skip execution on non-critical ticks.
