Wait, let's look at `GameObjectUtility.js`.
"Use `GameObjectUtility.getById(id)` as the standard method for retrieving game objects safely. It caches both successful retrievals and null results within a single tick"

Is there a `RoomMemoryUtility` or something?
